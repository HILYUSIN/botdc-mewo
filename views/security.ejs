<!DOCTYPE html>
<html>
<head>
    <title>Security & Roles</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <body class="bg-dark text-white p-4">
        <div class="container">
            <h2 class="text-info mb-4">👮 Security & Promosi Role</h2>
            <div class="alert alert-warning">
                Di bawah ini adalah <strong>Top 10 Member</strong> dengan XP Tertinggi.
                Klik tombol <strong>"Promote"</strong> untuk menjadikan mereka Ass. Mentor.
            </div>
            <table class="table table-dark table-striped">
                <thead><tr><th>Username</th><th>XP (Keaktifan)</th><th>Total Alpa</th><th>Aksi</th></tr></thead>
                <tbody>
                    <% candidates.forEach(m => { %>
                    <tr>
                        <td><%= m.username %></td>
                        <td class="text-success fw-bold"><%= m.xp %> XP</td>
                        <td class="text-danger"><%= m.totalAlpa %>x</td>
                        <td>
                            <form action="/promote" method="POST">
                                <input type="hidden" name="userId" value="<%= m.userId %>">
                                <button type="submit" class="btn btn-sm btn-warning">🔼 JADIKAN MENTOR</button>
                            </form>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
            <a href="/" class="btn btn-secondary">Kembali</a>
        </div>
    </body>
</html>